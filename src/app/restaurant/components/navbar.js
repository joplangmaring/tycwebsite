'use client'

import React from "react";
import Link from "next/link";
// import Logo from '../../public/tyclogo.png';
import Image from "next/image";

const Navbar = () => {
  return (
    <div>
      {/* <nav className={`flex justify-center items-center h-[100px] px-4 md:px-8 bg-black`}>
        <div className="mt-[4%]">
          <Link href='/'>
            <Image 
              src={Logo}
              alt="Logo"
              className="h-full" 
            />
          </Link>
        </div>
      </nav>  */}
    </div>
  );
};

export default Navbar;
